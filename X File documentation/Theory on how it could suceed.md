1 - We need to convert the file encoding to UTF-8 from Shift-JIS almost all software that can load .x files crashes with this encoding.

? - Convert the Binary file to text(ascii), this would not only make it easier to understand but probably solve all problems