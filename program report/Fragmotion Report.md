Crashes a lot when we try to load the x files, but (seems to) properly import, export and convert X files
Has no CLI, which makes it really annoying to use in a batch operation
its own batch mode crashes every time

cannot be bought anymore and theres no crack for it, but its free